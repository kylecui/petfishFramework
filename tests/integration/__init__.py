"""Integration package — tests that require real API keys.

These tests are marked @pytest.mark.integration and skipped by default.
Run with: pytest -m integration -v
Requires: OPENAI_API_KEY and/or ANTHROPIC_API_KEY environment variables.
"""
